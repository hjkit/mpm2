# Build artifacts
build/
**.rel
*.o
*.a
*.so
*.dylib

# Executables
mpm2_emu
mkboot
mkdisk

# Disk images (regenerated from source files)
disks/*.img
disks/*.dsk

# SSH keys (sensitive - generate locally)
keys/

# Editor/IDE files
*.swp
*.swo
*~
.vscode/
.idea/
*.sublime-*

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
compile_commands.json
Makefile

# SSH libraries (external dependencies)
# These must be installed separately by developers due to:
# - Copyright restrictions
# - Cryptographic export control regulations
# Install via: brew install libssh (preferred) or build wolfSSH
wolfssl/
wolfssh/
wolfssl-*/
wolfssh-*/
libssh/
libssh-*/

# Temporary files
*.tmp
*.temp
/tmp/

# Assembled binaries and intermediate files (regenerate with um80/ul80)
asm/*.bin
asm/*.hex
asm/*.lst
asm/*.prn
asm/*.rel
asm/*.SPR
asm/*.spr

# Generated boot images and system files
disks/*.bin
disks/*.sys
disks/*.com

# External MP/M II source/binaries
# Digital Research materials - now freely available for non-commercial use

# Patched system files
/tmp/MPM*.SYS

# macOS
.DS_Store

# Linux
*.core
